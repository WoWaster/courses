# Домашние задания по курсу

## ДЗ № 1. Настройка репозитория + CSV pretty printer

### Настройка репозитория

В прошлом семестре Вы познакомились с инструментами разработчика на Си.
Сейчас придётся использовать все полученные знания.

Обязательно нужно.
1. Создать новый репозиторий.
2. Настроить в нём CI с clang-format __И__ clang-tidy (пример по здесь: https://github.com/WoWaster/spbu-c-ci-example).
3. Использовать CMake.
4. Писать тесты к своему коду.
5. Не забыть про лицензию и хоть какой-нибудь README.md.

Если что-то забылось, всегда можно обратиться к материалам прошлого семестра: https://github.com/WoWaster/courses/tree/main/c_programming

Можно:
1. Подготовить репозиторий самостоятельно без отдельного PR.
2. Переиспользовать библиотеки из прошлого семестра.
3. Торговаться с преподавателем на тему отключения *странных* правил clang-tidy.

__ВАЖНО__: при отсутствии "зеленой галочки" велик шанс получить 0 баллов за задачу сразу же, без какой-либо проверки.


### CSV pretty printer

Требуется разработать консольную утилиту, преобразующую CSV файл (https://en.wikipedia.org/wiki/Comma-separated_values) `input.csv` в таблицу в формате псевдографики и записывающую её в файл `output.txt`.

Правила отрисовки:
- Заголовок должен быть выделен. Поля заголовка выравнены влево.
- Данные: текст — выравнен влево, числа — выравнены вправо.
- Ширина столбцов одинаковая и соответствует максимальному элементу столбца.

Пример выходного файла:
```
+===================+==============+
| Test field 1      | Test field 2 |
+===================+==============+
| test              |          123 |
+-------------------+--------------+
| long string test! |         28.7 |
+-------------------+--------------+
| other text        |            3 |
+-------------------+--------------+
```

Обратите внимание, __НЕ__ нужно поддерживать экранированные строки и многобайтовые кодировки.

Будет полезно изучить спецификаторы `printf`-подобных функций и, в частности, функцию `sprintf`.
(см. https://en.cppreference.com/w/c/io/fprintf.html, `man 3 printf` и `man 3 sprintf`)


## Практика по деревьям

Вам предстоит в командах по 2­-3 человека реализовать абстрактный тип данных "Множество" на основе бинарного дерева поиска.
Работа разделена на две части: на паре и дома.
Первую часть нужно сдать перед тем, как уйти с пары, у второй части обычный дедлайн.

Первая часть оценивается в 3 балла, вторая в 7.
Предполагается, что качество кода внутри команды вы контролируете сами, мы же контролируем только сам факт выполнения заданий.
**Однако**, за каждый обнаруженный факт кумовства или баг, автор PR получает -1 балл, а проверяющий -2 балла.

Дальше чуть более формальное описание работы (от Claude Sonnet 4.6 с редактурой от @WoWaster 😃).

### Организация работы

Работа выполняется в команде по 2–3 человека.
Первая часть выполняется на паре, вторая — дома.

**На паре:**
1. Создайте репозиторий на GitHub.
2. Договоритесь о структуре файлов и содержимом заголовочных файлов — все задачи будут опираться на общий интерфейс.
3. Решите задачи A, B, C. Можно совместно за одним ноутбуком (здесь будет полезно узнать, что такое `co-authored-by`), либо распараллелиться.
4. (можно делать дома) Настройте CI: он должен автоматически проверять код с помощью `clang-format` (стиль WebKit) и `clang-tidy` при каждом пуше и открытии PR (вы знаете, где искать пример 😉).

**Дома:**
1. Решите задачи D, E, F, G, H, распределив их между собой.
2. Каждую задачу реализуйте в отдельной ветке и откройте PR в main.
3. Каждый PR должен проверить хотя бы один другой член команды и оставить содержательный комментарий — не просто "LGTM", а конкретное наблюдение: корректность, стиль, граничные случаи.
4. В описании PR укажите: что реализовано, как проверяли, какие граничные случаи учли.
5. Решите задачу I.

### Работа в классе

#### Задача A — Структура и вставка

Опишите абстрактный тип данных — двоичное дерево поиска (`BST`) — и его структуру.

Реализуйте функции:
- `void bstInsert(BST* tree, int value)` — вставить значение в дерево; если значение уже есть, ничего не делать.
- `bool bstContains(BST* tree, int value)` — вернуть `true`, если значение есть в дереве.
- `void bstFree(BST* tree)` — освободить всю память, занятую деревом.

Напишите `main` с несколькими простыми проверками: вставьте несколько элементов и убедитесь, что `bstContains` возвращает правильные результаты.

#### Задача B — Обходы

Реализуйте функции обхода дерева, каждая из которых печатает элементы в соответствующем порядке. Все функции должны быть реализованы **рекурсивно**:
- `void bstInorder(BST* tree)`
- `void bstPreorder(BST* tree)`
- `void bstPostorder(BST* tree)`

Все функции должны корректно работать на пустом дереве.

#### Задача C — Статистика дерева

Реализуйте функции:
- `int bstHeight(BST* tree)` — высота дерева (пустое дерево имеет высоту 0).
- `int bstSize(BST* tree)` — количество узлов.
- `int bstMin(BST* tree)` — минимальное значение.
- `int bstMax(BST* tree)` — максимальное значение.

Для `bstMin` и `bstMax` определите поведение на пустом дереве и задокументируйте его в заголовочном файле.

### ДЗ № 2. Бинарное дерево поиска

#### Задача D — Удаление

Реализуйте функцию:
- `void bstDelete(BST* tree, int value)` — удалить узел с заданным значением.

Функция должна корректно обрабатывать все три случая:
- узел является листом,
- узел имеет одного потомка,
- узел имеет двух потомков.

Если значения в дереве нет, функция не должна падать или изменять дерево.

#### Задача E — Слияние двух деревьев

Реализуйте функцию:
- `BST* bstMerge(BST* tree1, BST* tree2)` — вернуть новое дерево, содержащее все элементы из обоих деревьев без дубликатов.

Исходные деревья не должны изменяться.

#### Задача F — Проверка корректности

Реализуйте функцию:
- `bool bstIsValid(BST* tree)` — вернуть `true`, если дерево является корректным BST.

Обратите внимание: проверка только соседних узлов (`left < root < right`) недостаточна — подумайте, почему, и реализуйте корректное решение.

#### Задача G — k-й минимальный элемент

Реализуйте функцию:
- `int bstKthMin(BST* tree, int k)` — вернуть k-й минимальный элемент дерева (k начинается с 1).

Определите поведение при некорректном `k` (например, `k <= 0` или `k > bstSize(tree)`) и задокументируйте его в заголовочном файле.

#### Задача H — Итератор

Реализуйте итератор, который обходит дерево в порядке `inorder` **без рекурсии** — используя дополнительные структуры данных.

Интерфейс:
```c
Iterator* iteratorInit(BST* tree);
bool iteratorHasNext(Iterator* it);
int iteratorNext(Iterator* it);
void iteratorFree(Iterator* it);
```

Итератор не должен изменять дерево. Поведение при вызове `iteratorNext` на исчерпанном итераторе определите сами и задокументируйте.

#### Задача I — Рефлексия

Представьте, что вас попросили реализовать АТД «Множество».
Изучите код, написанный вашей командой, и ответьте письменно в файле `REFLECTION.md` на следующие вопросы:

1. Какие функции войдут в публичный интерфейс множества, а какие останутся деталями реализации? Правда ли они могут иметь другое название?
2. Какие из написанных вами функций не имеют смысла в интерфейсе множества?
3. Что нужно изменить, чтобы можно было безболезненно заменить BST на другую реализацию (например, хеш-таблицу), не меняя код, который использует множество?

Данную задачу допустимо сдавать сразу в основную ветку, но не забывайте про `co-authored-by`.

*Подсказка для тех, кто дочитал до конца*: аккуратно реализованный итератор может помочь в решении других задач.
